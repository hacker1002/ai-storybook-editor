// use-current-locale.ts — Minimal locale hook stub for Humans feature.
// Returns 'en_US' until i18n sprint introduces real locale state.

const DEFAULT_LOCALE = 'en_US';

export function useCurrentLocale(): string {
  return DEFAULT_LOCALE;
}
