// constants.ts — Humans feature constants.

import type { HumansFilterState } from '@/types/human';

export const DEFAULT_HUMANS_FILTERS: HumansFilterState = { search: '' };
export const SEARCH_DEBOUNCE_MS = 200;
