// Barrel for the Creating-Image workspace modal. Keeps the import path stable
// (`./generate-image-modal`) for existing callers after the file → folder move.
export { GenerateImageModal } from "./generate-image-modal";
