// Public type aliases for the music domain. Canonical types live in
// `@/features/audio-library/types`.

export type {
  AudioResource as Music,
  AudioRow as MusicRow,
  AudioSource as MusicSource,
  AudioType as MusicType,
  AudioFilterState as MusicsFilterState,
  AudioActiveModal as MusicsActiveModal,
} from '@/features/audio-library/types';
